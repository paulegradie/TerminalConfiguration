# ~/.bashrc – managed by TerminalConfiguration setup-unix.sh
# Keep this minimal: delegate to ~/.bash_profile so there is one source of truth.

if [ -f "$HOME/.bash_profile" ]; then
  source "$HOME/.bash_profile"
fi

